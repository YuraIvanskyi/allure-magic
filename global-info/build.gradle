repositories {
    jcenter()
}

apply plugin: 'java-library-distribution'

jar {
    archiveName = 'plugin.jar'
}
java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(11)
    }
}
description = 'Allure Global Info Plugin'

dependencies {
    compileOnly('io.qameta.allure:allure-plugin-api:2.20.1')
}

// artifacts.add("allurePlugin", tasks.distZip)
artifacts.add("archives", tasks.distZip)